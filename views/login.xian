<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <script src="https://cdn.tailwindcss.com"></script>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
  <title>Login - SKONNECT Portal</title>
  <style>
    .fade-in {
      animation: fadeIn 0.5s ease-in-out;
    }
    @keyframes fadeIn {
      from { opacity: 0; transform: translateY(-10px); }
      to { opacity: 1; transform: translateY(0); }
    }
  </style>
</head>
<body class="bg-gray-50 font-sans min-h-screen flex flex-col">

  <!-- Header -->
  <header class="bg-gray-800 text-white shadow-lg">
    <div class="container mx-auto flex justify-center items-center p-4">
      <h1 class="text-2xl font-bold tracking-wide">
        <i class="fas fa-landmark mr-2"></i>SKONNECT Portal
      </h1>
    </div>
  </header>

  <!-- Main Content -->
  <main class="container mx-auto p-4 sm:p-6 lg:p-8 flex-grow flex items-center justify-center">
    <div class="w-full max-w-md">
      <form class="bg-white p-8 rounded-xl shadow-md fade-in" method="POST" action="/login">
        <h2 class="text-2xl font-semibold mb-6 text-center text-gray-800">
          <i class="fas fa-sign-in-alt mr-2 text-gray-700"></i>Official Login
        </h2>
        {{#if error}}
          <div class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded-lg relative mb-4" role="alert">
            <span class="block sm:inline">{{error}}</span>
          </div>
        {{/if}}
        <div class="mb-4">
          <label for="email" class="block text-gray-700 font-medium mb-2">Email Address</label>
          <input type="email" id="email" name="email" placeholder="you@example.com" value="{{email}}" class="w-full px-4 py-2 border rounded-lg focus:outline-none focus:ring-2 transition {{#if error}}border-red-500 focus:ring-red-500{{else}}focus:ring-gray-500{{/if}}" required />
        </div>
        <div class="mb-6">
          <label for="password" class="block text-gray-700 font-medium mb-2">Password</label>
          <input type="password" id="password" name="password" placeholder="••••••••" class="w-full px-4 py-2 border rounded-lg focus:outline-none focus:ring-2 transition {{#if error}}border-red-500 focus:ring-red-500{{else}}focus:ring-gray-500{{/if}}" required />
        </div>
        <button type="submit" class="w-full bg-gray-700 hover:bg-gray-800 text-white font-bold py-3 px-6 rounded-lg shadow-lg transform hover:scale-105 transition-all duration-300">
          Login
        </button>
        <p class="text-center text-gray-600 mt-6">
          Don't have an account? <a href="/sysregister" class="text-gray-700 hover:underline">Register here</a>
        </p>
        <p class="text-center text-gray-600 mt-2">
          <a href="/" class="text-sm text-gray-500 hover:text-gray-700 hover:underline">Go Back to Home</a>
        </p>
      </form>
    </div>
  </main>

 {{> myfoot}}

</body>
</html>